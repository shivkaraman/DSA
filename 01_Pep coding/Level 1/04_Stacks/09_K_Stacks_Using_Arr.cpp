/*
For this we use 3 arrays
    1. top[k]--> to store top of each stack
    2. next[n]--> To store the index of next free space in array
    3. data[n]--> Tp store data items of k stacks
Initialize all elements of top to -1 coz all stacks are empty initially
Initialise next[i] to i+1 coz all slots are free initially
*/

